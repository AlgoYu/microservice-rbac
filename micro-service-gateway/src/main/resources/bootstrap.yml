# 配置中心配置
spring:
  cloud:
    nacos:
      config:
        server-addr: development.machine-geek.cn:80
        namespace: Public
        group: DEFAULT_GROUP
        file-extension: yaml
        prefix: MICRO-SERVICE-GATEWAY